use std::fs::File;
use std::fs::Path ;
use std::io;
use std::io::BufReader ;
use std::io::prelude::* ;
use std::path::PathBuf;
use csv;



fn main() {
    let chk_file = PathBuf("../data/Checking1.csv");


}
